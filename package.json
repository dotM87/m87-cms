{
  "name": "flat-cms",
  "version": "1.0.0",
  "description": "Vanilla flat file CMS",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "clean": "rm -rf dist",
    "build": "pnpm run clean && node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "formidable": "^3.5.2",
    "gray-matter": "^4.0.3",
    "marked": "^15.0.7"
  }
}